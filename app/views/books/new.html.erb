<h1>New Book</h1>

<%= form_with(model: @book, url: books_path) do |form| %>
  <div>
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <%= form.submit "Create Book" %>
<% end %>
